info "[POST] Loading OMZ config for Linux" $(cur_path_relative "$HOME/.dot" "$0")
# This config file specific to Linux platform.
# You can set any variables ONLY to linux platform.

### Make neomutt happy
stty flush undef

info "[POST] Loading OMZ config for Linux - DONE" $(cur_path_relative "$HOME/.dot" "$0")
