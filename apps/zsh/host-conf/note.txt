This directory contains optional host-specific Zsh hooks. `pre.zsh` sources
`00-zshrc-pre.sh` when readable, and `post.zsh` does the same for
`00-zshrc-post.sh`.

The hook links are ignored by the public repository. Only these two explicit
names are loaded; other files in this directory are not sourced implicitly.
