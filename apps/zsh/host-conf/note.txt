This directory is reserved for optional host-specific Zsh settings. It is
currently disconnected: neither `pre.zsh` nor `post.zsh` sources files from
here.

The files are ignored by the public repository. When host configuration is
needed again, reconnect the appropriate phase explicitly rather than loading
everything in this directory implicitly.
